import styled from "styled-components";

const AnswerHeader = styled.div`
  padding: 16px;
`;

const AnswerCount = styled.h2`
  font-weight: 400;
  font-size: 1.3rem;
`;

export { AnswerHeader, AnswerCount };
