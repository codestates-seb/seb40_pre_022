spring:
  datasource:
    url: jdbc:mysql://stackoverflow-database-deploy.cmt8diwmv4pt.ap-northeast-2.rds.amazonaws.com:3306/stackoverflow?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: admin
    password: 1234
  jpa:
    hibernate:
      ddl-auto: validate  # (1) ??? ?? ??
    show-sql: true      # (2) SQL ?? ??
    properties:
      hibernate:
        format_sql: true  # (3) SQL pretty print

  mvc:
    servlet:
      path: /

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_USER}    # 환경변수에 gmail 아이디 / 비밀번호 추가.
    password: ${SMTP_PW}
    properties:
      mail.smtp.auth: ture
      mail.smtp.starttls.enable: true

mail:
  address:
    admin: admin@gmail.com

jwt:
  secret-key: ${JWT_SECRET_KEY}
  access-token-expiration-minutes: 15
  refresh-token-expiration-minutes: 30
